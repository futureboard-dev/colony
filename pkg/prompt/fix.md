Fix all {{.Gate}} errors in this codebase.

Errors:
{{.Errors}}

Rules:
- Fix ONLY these errors. Do not change any unrelated code.
- Do NOT run git, lint, format, or tests — the pipeline re-verifies after your fix.
- When fixed, stop.
